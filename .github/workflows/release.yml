name: Create release

on:
  push:
    tags: ["*"]

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  github:
    uses: tree-sitter/workflows/.github/workflows/release.yml@67eb38f65e3a08dc2851fa8575ced44ff2857705
    with:
      attestations: true
